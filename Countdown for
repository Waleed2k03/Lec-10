#include <iostream>
#include <string>
using namespace std;
int main()
{
    for (int x = 10; x >= 0; x--)
    {
        if (x != 0)
            cout << "x " << x << "\n";
        else
            cout << "we have lift off" << endl;
    }
}
